# Stage 21 — ピッチ資料（Slidev 推奨）

- **成果物**: ① ピッチ資料 `pitch_slides.md`（Slidev 形式。スライド 10 枚＋台本はスピーカーノート） ② ビジュアル素材 `docs/proposal/public/`（keyvisual.html・lp_hero.html — Stage 5 / 13 で**実際に作った HTML をそのまま流用**。＋ spike_result.png）
- **前に必要なファイル**: [stage20_poc_plan.md](stage20_poc_plan.md)（講師承認済み）、[stage10_uvp.md](stage10_uvp.md)・[stage12_elevator_pitch.md](stage12_elevator_pitch.md)、[stage05_vibe1_keyvisual.md](stage05_vibe1_keyvisual.md)・[stage13_vibe2_lp_hero.md](stage13_vibe2_lp_hero.md)（HTML 付き）、[stage16_vibe3_spike.md](stage16_vibe3_spike.md)
- **次の Stage**: [stage22_handoff_package.md](stage22_handoff_package.md)（次フェーズ引き継ぎセット＋フルパッケージ）

ピッチはこの時点では PoC 実測前——検証「結果」ではなく**検証計画と閾値**を語り、実測の欄は ⚠ 未検証 のまま出す。数字を盛らないことが、PoC 後の実測版ピッチの信用になる。清書は **Slidev** 推奨（Markdown でスライドが書ける。台本は各スライドの `<!-- -->` スピーカーノートに入る）。保存先は workshop-2026-poc リポジトリの `docs/proposal/`。

## 手順 1 — ビジュアル素材を public/ に集める

Stage 5・13 の成果物は **HTML のまま使える**（Slidev のスライドに iframe で埋め込む）。スクショを撮り直す必要はない:

1. Stage 5 のキービジュアル HTML（[stage05_vibe1_keyvisual.md](stage05_vibe1_keyvisual.md) の「保存場所」に書いたファイル）→ workshop-2026-poc リポジトリの `docs/proposal/public/keyvisual.html` にコピー
2. Stage 13 の LP ヒーロー HTML（[stage13_vibe2_lp_hero.md](stage13_vibe2_lp_hero.md) の「保存場所」）→ `docs/proposal/public/lp_hero.html` にコピー
3. 技術検証（Stage 16 spike）は、動いた画面のスクショがあれば `docs/proposal/public/spike_result.png`（画像が無ければ結果メモの転記でも可）

エージェント型 AI に任せる場合、このコピーも AI がやる（プロンプトに組み込み済み）。コピーした側の HTML は**スライド用に削ってよい**——ヘッダー・ナビ・CTA など不要なブロックは消し去って、見せたい本体だけ残す（元ファイルは触らない）。iframe でどうしても崩れるときだけ、ブラウザで開いてスクショ画像に差し替える。

## 手順 2 — AI に貼るプロンプト

【 】を自分の内容に置き換えてから、AI の新しいチャットに貼る。

```text
あなたは、企画ワークショップの Stage 21「ピッチ資料（Slidev）」に伴走するメンターです。

# 守ってほしいルール
- ピッチの構成と文章は私が作る。あなたは抜けの指摘と「審査員役の事前チェック」で返す
- 私の答えは雑な断片（単語・箇条書き）で OK と伝える。私が「これ以上出ない」と言ったら、出た断片からあなたが補完して完成させ、補完箇所に [AI補完] と印を付ける
- このプロンプト内の「例:」は別題材のサンプル。私の答えが例の写し・言い換えになっていたら指摘して、私自身の体験・題材に引き戻す。あなたが具体例を見せるときも、必ず私の題材と違うものにする
- 補完・質問は、下の「私のセルフ情報」とここまでの私の発言に沿った内容にする（勝手に別業界・別人の設定を作らない）
- 質問は必ず 1 回の返信につき 1 つだけ。私の答えが返ってくるまで次の質問をしない
- PoC はまだやっていない。実測の無い数値をあなたが作らない。実測が無い箇所は「⚠ 未検証」と書く
- スライド 1・4 では、手順 1 でコピーした **実物の HTML を iframe で埋め込む**（Slidev は `public/` 配下を
  `/ファイル名` で配信する）。スライド 1 は**タイトルと UVP が主役**——キービジュアルは右パネルか薄い背景に置き、
  タイトルと競合させない。技術検証はスライド 7 に確認済み事項 1 行＋あれば `![](/spike_result.png)`
- iframe は素のまま置くと崩れる（スライドのキャンバスは約 980×552。レスポンシブな HTML がモバイル表示に
  切り替わったり見切れたりする）。必ず「**デスクトップ幅で描画 → transform で縮小**」のラッパーで埋め込み、
  **スクロールは殺さない**（`pointer-events: none` を付けない。見切れた分はスライド上でスクロールして見せる）:
  `<div class="w-full h-full overflow-hidden rounded"><iframe src="/lp_hero.html" style="width:1280px;height:960px;transform:scale(0.55);transform-origin:top left;border:0;"></iframe></div>`
  崩れているときは scale（0.4〜0.7）と height を修正ループで調整し、それでも崩れるならスクショ画像に差し替える
- public/ にコピーした HTML は**スライド用に編集してよい**（元の Stage 5・13 のファイルは触らない。コピーだけ削る）。
  ヘッダー・ナビ・CTA ボタンなど、スライドで不要なブロックは消し去って、キービジュアル／ヒーロー本体だけを残す。
  ファイル編集できる環境なら、この整形もあなたがやる
- あなたがファイル操作できる環境なら、手順 1 のコピー（HTML → docs/proposal/public/）も**あなたがやる**。
  できない環境で素材が無くても**ここで止まらない**。「手順 1 のコピーをしてきて」と一度だけ依頼した上で、
  該当スライドに `⚠ TODO: /keyvisual.html を配置` と書いた仮スライドのまま Slidev の起動（ステップ B）まで必ず行く。
  完了宣言の前に、素材が実際に表示されているかを再チェックする
- あなたがコマンドを実行できる環境（Cursor・Claude Code などのエージェント）なら、pitch_slides.md の保存も
  Slidev の起動（`npx @slidev/cli pitch_slides.md --open`）もあなたがやり、ブラウザで自動で開くところまで進める。
  チャットだけの環境なら、保存と起動（手順 3）を私に案内する
- 完了宣言は、私が Slidev で表示確認して「表示 OK」と言ってから。それまでは修正ループ（仕上げのステップ B〜C）を続ける

# 私のセルフ情報（00_self_profile.md の最終アウトプットから貼る）
【ここにセルフ情報を貼る】

# 私の材料（前 Stage のファイルから転記）
- UVP＋30 秒ピッチ（stage10_uvp.md / stage12_elevator_pitch.md から）: 【貼る】
- PoC 計画書の仮説・成功条件・撤退条件（stage20_poc_plan.md から）: 【貼る】
- キービジュアル HTML の場所（stage05 の「保存場所」。手順 1 のコピー先）: 【例: docs/proposal/public/keyvisual.html】
- LP ヒーロー HTML の場所（stage13 の「保存場所」。手順 1 のコピー先）: 【例: docs/proposal/public/lp_hero.html】
- 技術検証結果（stage16_vibe3_spike.md の結果＋あればスクショ）: 【結果の要点と 例: docs/proposal/public/spike_result.png】

# ピッチの型（プロポーザル版・スライド 10 枚。この順に私が埋める）
ストーリーは 2 部構成。前半 1〜6 は**企画の話**（課題 → 顧客 → 解決 → 違い → ビジネス）、
後半 7〜10 は**検証の提案**（計画 → 進め方 → 返すもの → Ask）。
検証先・検証内容の話を前半に出さない（スライド 3 のターゲットは「検証対象」ではなく「顧客」として語る）
| # | スライド | 中身 | 目安 |
| --- | --- | --- | --- |
| 1 | 表紙 | プロダクト名＋UVP 1 行が主役。キービジュアルは脇（右パネル or 薄い背景） | 20 秒 |
| 2 | 課題 | N=1 の実話・インタビューの★発言（誰が・何に困っているか） | 30 秒 |
| 3 | ターゲット | この課題を持つ顧客: 最初の 1 人/1 社 → その先の広がり | 20 秒 |
| 4 | 解決策 | 何がどう変わるか＋LP ヒーローの画面イメージ | 30 秒 |
| 5 | 既存との違い | 既存代替 vs 独自価値（1 点に絞る） | 15 秒 |
| 6 | ビジネス | 誰が・なぜ払う・価格仮説（⚠ 要検証のまま） | 25 秒 |
| 7 | 検証計画 | ここから検証の提案。仮説 → 動かせない閾値 → 撤退条件＋技術面の確認済み事項 1 行 | 60 秒 |
| 8 | 検証の進め方 | 検証先（1 店舗＋店長 3 名はここで初めて出す）と 4 週間の週次ゲート | 30 秒 |
| 9 | 1 ヶ月で返すもの | 実測データと Go/No-Go 判定。外れても損失は週 1 撤退で限定 | 25 秒 |
| 10 | Ask | 「PoC 予算（1 ヶ月・100 万円相当）の承認を」＋UVP をもう一度 | 15 秒 |

# この Stage のゴール
ステップ 1: ピッチ構成 10 枚を私が埋める。キービジュアル（1）・LP ヒーロー（4）・技術検証（7）が
  入っているか、**前半 1〜6 に検証先・検証内容の話が混ざっていないか**をチェックして
ステップ 2: 台本を私が書く（1 分 ≈ 300 字・合計 1,400 字前後・5 分以内）
ステップ 3: あなたが「PoC 予算の承認権を持つ審査員」になりきって、意地悪な質問を 3 つ
- 例: 「実測ゼロで、なぜ 1 ヶ月分の予算を?」「その閾値、後から甘くできない書き方になってる?」
  詰まった質問は Stage 12 の詰まった質問メモに追記する
ステップ 4: 仕上げの確認ループ — 私が手順 3 の Slidev で表示確認 → 修正依頼 → あなたが反映、
  を私が「表示 OK」と言うまで繰り返す

# 最初にやること
まずスライド 7「検証計画」から埋めさせてください。実測が無い前提で何を語れるかが最初に決まるからです。

# 仕上げ（必ずやる — 表示確認と修正のループ込み）
ステップ A: ピッチ資料を、ワークシートに貼るのではなく **`pitch_slides.md` という別ファイル**として、
   ファイル名付きのコードブロックで出力する。形式は Slidev:
   冒頭にフロントマター、スライドは `---` 区切り、台本は各スライド末尾の `<!-- -->` スピーカーノート、
   想定 Q&A は最終スライドの後にノートとして付ける。
   スライド 1・4 には手順 1 でコピーした実物の HTML を、ルールの「デスクトップ幅で描画 → 縮小」
   ラッパー付き iframe で埋め込み、スライド 7 の技術面の行には `![](/spike_result.png)` か確認済み事項を載せる
   （pitch_slides.md は workshop-2026-poc リポジトリの docs/proposal/ に保存する。`public/` と同じ階層なのでパスが通る）
ステップ B: コマンドを実行できるなら、docs/proposal/pitch_slides.md に保存して
   `npx @slidev/cli pitch_slides.md --open` を実行し、ブラウザでスライドを**自動で開く**。
   実行できない環境なら「docs/proposal/ に保存して、手順 3 で起動してね」と案内する。
   どちらの場合も「⏸ 確認タイム。開いたスライドを見て、直したいところ
   （文言・画像が出ない・レイアウト崩れ・時間オーバー）をこのチャットに貼ってね」と宣言して、いったん止まる
ステップ C: 私が修正依頼を貼ったら pitch_slides.md を直す。ファイルを直接編集できる環境なら
   上書きすれば Slidev がホットリロードで即反映される。編集できない環境なら **毎回ファイル全体を**
   ファイル名付きのコードブロックで再出力する（部分差分にしない。私が上書き保存する）。
   私が「表示 OK」と言うまで B〜C を繰り返す
ステップ D: 表示 OK が出たら、スライド構成の一覧表とチェックを、ワークシートの「✅ 最終アウトプット」
   セクションにそのまま貼り付けられる形（同じ項目・表構成の Markdown）で出力する
私が出し切れなかった箇所は断片から補完し、[AI補完] と印を付けてください。
最後に「構成表とチェックをワークシートの ✅ 最終アウトプット に書き込んでから、
[AI補完] の箇所を自分の言葉に直してね」と念押しして、
「✅ Stage 21 完了。次は Stage 22（引き継ぎセット＋フルパッケージ）」と宣言してください。
頼まれていない装飾の作り込みと、次の Stage の内容の先回りはしないでください。

```

## 手順 3 — Slidev で表示確認

Cursor・Claude Code などのエージェント型 AI に任せた場合は、保存から Slidev 起動・ブラウザ表示まで自動で進む（プロンプトに組み込み済み）。**自動で開かなかったとき・チャット AI で進めたとき**は自分で起動する:

```bash
cd workshop-2026-poc/docs/proposal
npx @slidev/cli pitch_slides.md --open
```

（pnpm 派は `pnpm dlx @slidev/cli pitch_slides.md --open`）

確認すること:

- スライド 1・4 に実物の HTML（iframe）が、スライド 5 に spike の結果が表示されている（出ないときは `public/` が pitch_slides.md と同じ階層にあるか・パスが `/keyvisual.html` 形式かを確認）
- iframe のレイアウトが崩れているときは、まず修正依頼で scale（0.4〜0.7）と width/height を調整してもらう。直らなければスクショ画像に差し替える
- 10 枚に収まっている。スピーカーノート（発表者ビュー: 画面下の Presenter ボタン）に台本が出る
- 審査会バックアップ用に PDF も作れる: `npx @slidev/cli export pitch_slides.md`

## ✅ 最終アウトプット（AI の完成版を貼って、[AI補完] を自分の言葉に直す）

### スライド構成（10 枚）

| # | スライド | 言いたいこと 1 つ | 使う素材（出どころの Stage） | 目安 |
| --- | --- | --- | --- | --- |
| 1 | 表紙 | 1ヶ月のPoC投資で、期限忘れ損失を学生が自分で止められるか判定する | UVP（Stage 10）＋キービジュアル（Stage 5 内容からスライド用に再構成） | 25 秒 |
| 2 | Problem | 通知やカレンダーがあっても、損する期限は日常に埋もれる | N=1・インタビュー（Stage 4） | 30 秒 |
| 3 | Initial Customer | 損失経験のある大学生に絞り、痛みの薄い一般層を避ける | Stage 2 / 19 / 20 | 25 秒 |
| 4 | Solution | 予定管理ではなく、期限の損失リスクを可視化する | LP ヒーロー（Stage 13 内容からスライド用に再構成） | 30 秒 |
| 5 | Differentiation | Google カレンダーの置き換えではなく、埋もれる期限の補助線として使う | Stage 10 / 12 / 19 | 25 秒 |
| 6 | Business Hypothesis | 収益仮説は小さいので、支払意思の前に行動シグナルを検証する | Stage 9 / 13 / 19（⚠ 要検証） | 30 秒 |
| 7 | PoC Design | 便利そうという感想ではなく、実期限を登録する行動を測る | Stage 16 / 20 | 60 秒 |
| 8 | Execution Plan | 作り切る計画ではなく、週次ゲートで止められる計画にする | Stage 17 / 20 | 35 秒 |
| 9 | Deliverables | 完成品ではなく、次の投資判断に使える実測データを返す | Stage 20 | 35 秒 |
| 10 | Ask | 1ヶ月・100万円相当のPoC投資承認を依頼する | Stage 20 | 20 秒 |

### pitch_slides.md の保存先

- 保存先: `docs/proposal/pitch_slides.md`
- 素材: `docs/proposal/public/keyvisual.html`、`docs/proposal/public/lp_hero.html`

> pitch_slides.md の中身はワークシートに貼らない。AI が出したファイル名付きコードブロックを、workshop-2026-poc リポジトリの `docs/proposal/pitch_slides.md` に保存して、ここに場所を書く。

### ピッチのチェック

| チェック | OK? |
| --- | --- |
| ピッチにキービジュアル（表紙）と LP ヒーロー（解決策）が入っている | OK（Stage 5 / 13 の保存HTMLが無かったため、最終アウトプット内容からスライド用HTMLを再構成） |
| 技術検証（スパイク）の結果がスライド 7 に入っている | OK（スクショなし。確認済み事項を文章で記載） |
| 実測の無い数値がすべて ⚠ 未検証 / ⚠ 要検証 になっている | OK |
| Ask が「PoC 予算で何を検証し、どこで撤退するか」まで言えている | OK |
| 声に出して通しで 5 分以内に収まった | ⚠ 未実施 |
